<%- assert_locals gem -%>
<%= render partial: "shared/header" -%>

require "minitest/autorun"
require "minitest/focus"
require "minitest/mock"
require "minitest/rg"

require "grpc"

require "ostruct"
