syntax = "proto3";

package testing.google.example;
option ruby_package = "Testing::Google::Example";

service Example {
    option (google.api.default_host) = "grpcserviceconfig.example.com";
    rpc ExampleMethod(Request) returns(Response);
}

message Request {
    string name = 1;
}

message Response {
}
