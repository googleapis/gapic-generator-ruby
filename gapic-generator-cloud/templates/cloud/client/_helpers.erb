# rubocop:disable Lint/HandleExceptions

# Once client is loaded, load helpers.rb if it exists.
begin
  require "<%= helpers_require service %>"
rescue LoadError
end

# rubocop:enable Lint/HandleExceptions
