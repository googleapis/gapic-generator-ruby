# Garbage Protos

These protos are intended to be used to provide full coverage of multiple
aspects that a protobuf can expose. This includes properly documenting all
possible field types, all RPC method types (client, server, bidi), and all
protobuf annotations.
